#!/bin/sh
curl https://cloud.lambda.ai/api/v1/instances -s -u "$LAMBDA_API_KEY" |
	jq -r '.data[] | [
		.id,
		.ip,
		.status,
		.instance_type.description,
		.region.description,
		.instance_type.price_cents_per_hour/100,
		.ssh_key_names[]
	] | @tsv'
